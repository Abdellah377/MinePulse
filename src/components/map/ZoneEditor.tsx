/**
 * Zone editor shell — UI panels live in ZoneEditorPanel; map drafting via ZoneDraftLayer.
 * Explicit “Modifier les zones” mode is toggled from MapControls / Carte.
 */
export {
  ZoneEditorToolbar,
  ZoneListPanel,
  ZonePropertiesPanel,
  type ZoneDraft,
} from "@/components/map/ZoneEditorPanel"
